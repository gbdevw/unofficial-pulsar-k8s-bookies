# Default class for local persistent volumes
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: standard-local
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
